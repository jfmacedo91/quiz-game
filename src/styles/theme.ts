import { createTheme } from "@material-ui/core";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#30BF78'
    },
    secondary: {
      main: '#308FBF'
    }
  },
  typography: {
    fontFamily: 'Signika'
  }
})